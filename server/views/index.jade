extends layout

block content
    head
        title=title
        meta(name="viewport", content="width=device-width", initial-scale=1)
        != styleContent

    body
        div#content
            != bodyContent

            != scriptsContent